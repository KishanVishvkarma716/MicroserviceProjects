spring:
  application:
    name: orders-service-with-kafka

  datasource:
    url: jdbc:mysql://localhost:3306/orderdb_dev?createDatabaseIfNotExist=true   # changed from mysql → localhost
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
